NODE_ENV=development
PORT=5002

PGHOST=171.237.176.73
PGPORT=5432
PGDATABASE=THLA
PGUSER=claude_agent_mes
PGPASSWORD=31072002
PGSSL=false

JWT_SECRET=thla_mes_dev_secret_change_me_2026
JWT_EXPIRES=365d

CORS_ORIGIN=http://localhost:3000

# ERP — URL API lấy phiếu nhận vải (đầy đủ path; code chỉ nối thêm ?fromDate=).
# LOCAL/LAN dùng IP private .34 ; SERVER public dùng IP public 118.69.134.179 (sửa trong .env của server).
ERP_PHIEU_NHAN_VAI_URL=http://10.84.40.34:5000/api/server/backup/mes/phieu-nhan-vai-60

# Job tự đồng bộ ERP mỗi giờ: TẮT ở local, BẬT ở server.
# .env được gitignore nên mỗi môi trường giữ giá trị riêng (không bị git pull ghi đè).
ERP_SYNC_ENABLED=false
